#compdef venv-up

_venv-up() {
  local curcontext="$curcontext" state line
  typeset -A opt_args

  _arguments \
    '1: :->session'

  case $state in
    session)
      _arguments "1:sessions:($(ls {{ pyvenvs.root_dir }}))"
    ;;
  esac
}

_venv-up "$1"
