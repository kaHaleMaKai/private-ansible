- hosts: my-computers
  vars:
    reinstall_awesome: no
    reinstall_heidi: no
    reinstall_vimrc: no
    home_dir: /home/lars
    awesome:
      config_dir: "{{ home_dir }}/.config/awesome"
      theme_dir: "{{ home_dir }}/.config/awesome/themes"
  roles:
  - role: desktop-commons
    sudo: yes
    tags: desktop-commons
    remote_user: lars
  - role: gitconfig
    sudo: no
    tags: git
    remote_user: lars

